/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['github.com', 'jonathanvargas0111.github.io', 'fakestoreapi.com'],
    },

}

module.exports = nextConfig
